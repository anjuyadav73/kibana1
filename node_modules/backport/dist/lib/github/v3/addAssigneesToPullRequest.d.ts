export declare function addAssigneesToPullRequest({ githubApiBaseUrlV3, repoName, repoOwner, accessToken, autoAssign, interactive, dryRun, pullNumber, assignees, }: {
    githubApiBaseUrlV3?: string;
    repoName: string;
    repoOwner: string;
    accessToken: string;
    autoAssign: boolean;
    interactive: boolean;
    dryRun?: boolean;
    pullNumber: number;
    assignees: string[];
}): Promise<void>;

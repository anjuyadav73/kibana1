import { Commit } from '../entrypoint.api';
import { ValidConfigOptions } from '../options/options';
export declare type CommitAuthor = {
    name: string;
    email: string;
};
export declare function getCommitAuthor({ options, commit, }: {
    options: ValidConfigOptions;
    commit: Commit;
}): CommitAuthor;
